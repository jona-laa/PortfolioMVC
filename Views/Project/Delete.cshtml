@model PortfolioMVC.Models.Project

@{
    ViewData["Title"] = "Project - Delete";
}

<main>
    <section id="edit-items" class="bg-light section-padding">
        <h2>Projects</h2>
        <div class="divider"></div>
        <div id="edit-items_container" class="row trio">
            <div class="portfolio-item">
                <img src="/images/uploads/portfolio/@(Model.ImageName)" asp-append-version="true" alt="">
                <h3>@Model.Title</h3>
                <a href="@Model.Url" target="_blank">@Model.Url</a>
                <p>@Model.Description</p>

                <form asp-action="Delete">
                    <input type="hidden" asp-for="Id" />
                    <input type="submit" value="Delete" class="btn btn-danger" />
                </form>
            </div>
        </div>
        <div class="go-back">
            <a asp-action="Index">Back to List</a>
        </div>
    </section>
</main>

<partial name="_AdminScripts" />
